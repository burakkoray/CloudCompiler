#include <stdio.h>
#include "functions.h"
int main()
{
     int a, b;
     printf("Adding two numbers two numbers: ");
     a=6;
     b=1;
     printf("%d + %d = %d", a, b, Sum(a, b));
   
 
	
	return 0;
}